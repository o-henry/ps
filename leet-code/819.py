def solution(p, b):
    # hash-map
    map = [w for w in range(1, len(p))]
    print(map)

    return


paragraph = "Bob hit a ball, the hit Ball flew far after it was hit."
banned = ["hit"]
print(solution(paragraph, banned))
